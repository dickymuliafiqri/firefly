/**
 * Application build and version constants.
 */
export const APP_VERSION = 'v1.17.0';

/**
 * Outbound project links, surfaced in the footer, the header (xl and up only —
 * narrower viewports have no spare header room) and the mobile navigation panel.
 */
export const GITHUB_URL = 'https://github.com/dickymuliafiqri/firefly';

/** Donation page. While empty, every support entry point stays hidden. */
export const SUPPORT_URL = 'https://trakteer.id/dickymuliafiqri';
